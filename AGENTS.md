# AGENTS.md

**语言协议**。与工具/模型交互：**英语**；与用户交互：**中文**。
探索代码使用 ace-tool MCP。
mvn 使用时添加 `-T 1.5C` 参数，开启多线程。
编码前先描述方案并等待批准，需求不明时提出澄清问题。
代码完成后列出潜在问题并提供测试用例。
发现 bug 时先编写复现测试，修复至测试通过。
每次被纠正后在项目 AGENTS.md 添加新规则避免重复错误。
修改 URL 改写逻辑时，必须覆盖“输入已经是缓存 URL” 的幂等场景，避免再次代理导致嵌套缓存链接。
执行 git commit / push 前，必须先核对当前仓库的 `user.name` 与 `user.email` 是否符合本次仓库要求，避免用错提交者身份。
